kcp_version: "0.10"
project: xorcery-alchemy
version: 0.4.2-SNAPSHOT
updated: "2026-03-15"
language: en
license: "Apache-2.0"
indexing: private

units:

  - id: crucible
    path: xorcery-alchemy-crucible
    intent: "What is the Alchemy processing engine? The crucible orchestrates transmutation pipelines — executes configured sequences of input → transform → output steps. Start here to understand how an ETL pipeline is defined and run."
    scope: global
    audience: [developer, agent]
    validated: "2026-03-15"
    depends_on: [jar-core]
    triggers: [crucible, ETL pipeline, transmutation, orchestration, pipeline execution, processing engine, input transform output]

  - id: jar-core
    path: xorcery-alchemy-jar
    intent: "What is the base transmutation contract? Core JAR handling and transformation infrastructure — the abstract transmutation lifecycle (open, transmute, close) that all concrete format modules implement."
    scope: module
    audience: [developer, agent]
    validated: "2026-03-15"
    triggers: [jar, transmutation, base contract, lifecycle, abstract, transmute, core infrastructure]

  - id: cabinet
    path: xorcery-alchemy-cabinet
    intent: "What file formats and integration connectors does Alchemy support? The cabinet holds all concrete transmutation implementations — CSV, Excel, YAML readers, Neo4j projection, OpenSearch indexing, GraphQL event publishing, JSLT and JavaScript transforms."
    scope: module
    audience: [developer, agent]
    validated: "2026-03-15"
    depends_on: [jar-core]
    triggers: [cabinet, CSV, Excel, YAML, Neo4j, OpenSearch, GraphQL, JSLT, JavaScript, connectors, file formats, integrations, readers, writers]

  - id: bom
    path: xorcery-alchemy-bom
    intent: "What are the Alchemy dependency versions? Maven BOM and parent POM — ensures consistent dependency versions across all Alchemy modules targeting Java 21."
    scope: module
    audience: [developer, agent]
    validated: "2026-03-15"
    triggers: [BOM, Maven, dependencies, versions, parent POM, Java 21, dependency management]

  - id: tests
    path: xorcery-alchemy-test
    intent: "How are Alchemy pipelines integration-tested? End-to-end test suite validating complete transmutation pipelines across all supported input formats and output integrations."
    scope: module
    audience: [developer, agent]
    validated: "2026-03-15"
    depends_on: [crucible, cabinet]
    triggers: [test, integration test, pipeline test, end-to-end, validation, transmutation test]

relationships:
  - from: crucible
    to: jar-core
    type: depends_on
  - from: cabinet
    to: jar-core
    type: depends_on
  - from: tests
    to: crucible
    type: depends_on
  - from: tests
    to: cabinet
    type: depends_on
